# DiRotQ requirements (CUDA 12.4)
# Install PyTorch with CUDA support first:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#
# Then install the remaining dependencies:
#   pip install -r requirements.txt

# Core ML
torch==2.6.0

# Diffusion / HuggingFace (diffusers also pulls in transformers, tokenizers, sentencepiece)
diffusers==0.36.0
transformers==4.57.6
accelerate==1.12.0
huggingface_hub==0.36.2
safetensors==0.7.0
tokenizers==0.22.2
sentencepiece==0.2.1

# Utilities
numpy==2.3.5
tqdm==4.67.3
pillow==12.0.0
pyyaml>=6.0
